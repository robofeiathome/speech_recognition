# import speech_recognition as sr

# # Defina o nome do arquivo de áudio
# audio_file = "gravacao.wav"

# # Crie um objeto Recognizer
# r = sr.Recognizer()

# # Carregue o arquivo de áudio
# with sr.AudioFile(audio_file) as source:
#     # Ajuste o recognizer para o áudio gravado
#     r.adjust_for_ambient_noise(source)
#     print("Transcrevendo o arquivo de áudio. Aguarde...")
#     # Faça a leitura do áudio com a biblioteca SpeechRecognition
#     audio_data = r.record(source)
#     # Transcreva o áudio para texto usando a API do Google Speech to Text
#     text = r.recognize_whisper(audio_data, language="english")
#     print("Transcrição completa:")
#     print(text)

import whisper

model = whisper.load_model("base")
result = model.transcribe("audio.mp3")
print(result["text"])
